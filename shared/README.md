Shared folder for types and utilities used across frontend and backend.

Ideas:
- /shared/types (DTOs)
- /shared/constants

Use package/build tooling (tsconfig path mappings or a tiny build) to share types between apps.
